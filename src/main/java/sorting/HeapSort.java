package sorting;

public class HeapSort {
	
	public void HeapingSort(int[] array) {
		// leftchild from parent = 2n
		// rightchild from parent = 2n + 1
		int size = array.length;
		for (int i = size; i > 0; i --) {
			
		}
	}
	
	private void constructMaxHeap(int[] array) {
		
	}
	
	private void swap(int[] array, int i, int j) {
		int temp = array[i];
		array[i] = array[j];
		array[j] = temp;
	}
	
}
